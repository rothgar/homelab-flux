machine:
  kubelet:
    extraMounts:
      - destination: /var/mnt/u-local-data
        options:
          - bind
          - rshared
          - rw
        source: /var/mnt/u-local-data
        type: bind
