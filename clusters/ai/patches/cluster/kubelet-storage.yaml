machine:
  kubelet:
    extraMounts:
      - destination: /var/mnt/u-longhorn
        options:
          - bind
          - rshared
          - rw
        source: /var/mnt/u-longhorn
        type: bind
