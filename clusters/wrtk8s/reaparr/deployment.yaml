apiVersion: apps/v1
kind: Deployment
metadata:
  name: reaparr
  namespace: reaparr
spec:
  replicas: 1
  selector:
    matchLabels:
      app: reaparr
  template:
    metadata:
      labels:
        app: reaparr
    spec:
      initContainers:
        - name: fix-permissions
          image: busybox:1.38
          command: ["sh", "-c", "chown -R 911:1001 /Config /Downloads && chmod -R u+rw /Config"]
          securityContext:
            runAsUser: 0
          volumeMounts:
            - name: config
              mountPath: /Config
            - name: downloads
              mountPath: /Downloads
      securityContext:
        fsGroup: 1001
      containers:
        - name: reaparr
          image: reaparr/reaparr:latest
          ports:
            - containerPort: 7000
          env:
            - name: TZ
              value: "America/Los_Angeles"
            - name: ResetCredentials
              value: "true"
          volumeMounts:
            - name: config
              mountPath: /Config
            - name: downloads
              mountPath: /Downloads
            - name: movies
              mountPath: /Movies
            - name: tvshows
              mountPath: /TvShows
      volumes:
        - name: config
          persistentVolumeClaim:
            claimName: reaparr-config-nfs
        - name: downloads
          persistentVolumeClaim:
            claimName: reaparr-downloads-nfs
        - name: movies
          persistentVolumeClaim:
            claimName: movies
        - name: tvshows
          persistentVolumeClaim:
            claimName: tvshows
---
apiVersion: v1
kind: Service
metadata:
  name: reaparr
  namespace: reaparr
spec:
  selector:
    app: reaparr
  ports:
    - port: 7000
      targetPort: 7000
