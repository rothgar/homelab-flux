apiVersion: v1
kind: Service
metadata:
  name: ctrld
  namespace: ctrld
spec:
  type: ClusterIP
  selector:
    app: ctrld
  ports:
    - port: 53
      targetPort: dns-udp
      protocol: UDP
      name: dns-udp
    - port: 53
      targetPort: dns-tcp
      protocol: TCP
      name: dns-tcp
