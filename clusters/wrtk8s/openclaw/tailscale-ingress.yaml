apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: openclaw-tailscale
  namespace: openclaw
spec:
  ingressClassName: tailscale
  rules:
    - host: openclaw
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: openclaw
                port:
                  number: 18789
  tls:
    - hosts:
        - openclaw
