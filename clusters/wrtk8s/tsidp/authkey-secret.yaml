---
# Placeholder — replace with an ExternalSecret pulling from 1Password
# once you decide on the auth-key strategy (reusable auth key vs OAuth
# client secret; see clusters/wrtk8s/tsidp/README.md if you add one).
# For now, create the real value out-of-band with:
#   kubectl -n tsidp create secret generic tsidp-authkey \
#     --from-literal=TS_AUTHKEY=tskey-auth-xxxxxxxxxxxxxxxx
# Flux won't overwrite because this manifest sets stringData:{} (empty
# merge patch) — the out-of-band-set value survives reconciles.
apiVersion: v1
kind: Secret
metadata:
  name: tsidp-authkey
  namespace: tsidp
type: Opaque
stringData: {}
