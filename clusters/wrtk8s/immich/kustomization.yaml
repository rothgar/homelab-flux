apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - source.yaml
  - library-pvc.yaml
  - pictures-pvc.yaml
  - postgres.yaml
  - iscsi-postgres-pvc.yaml
  - release.yaml
  - httproute.yaml
  - tailscale-ingress.yaml
  - external-secret.yaml
  # KEDA HTTP add-on CRD (httpscaledobjects.http.keda.sh) is installed;
  # this scaler is safe to enable.
  - ml-scaler.yaml
