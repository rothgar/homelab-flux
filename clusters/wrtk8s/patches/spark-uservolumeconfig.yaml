---
# Dedicated partition for LLM weight caches (HF_HOME, vLLM download-dir).
# Mounted at /var/mnt/model by Talos.
apiVersion: v1alpha1
kind: UserVolumeConfig
name: model
volumeType: partition
provisioning:
  diskSelector:
    match: disk.transport == "nvme"
  maxSize: 1TiB
  minSize: 800GiB
