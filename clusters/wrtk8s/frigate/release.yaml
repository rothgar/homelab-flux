apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: frigate
  namespace: frigate
spec:
  interval: 5m
  chart:
    spec:
      chart: frigate
      version: "7.8.0"
      sourceRef:
        kind: HelmRepository
        name: frigate
        namespace: frigate
  values:
    image:
      tag: 0.17.0
    persistence:
      config:
        enabled: true
        existingClaim: frigate-config-nfs
      media:
        enabled: true
        existingClaim: frigate-media-nfs
    affinity:
      nodeAffinity:
        requiredDuringSchedulingIgnoredDuringExecution:
          nodeSelectorTerms:
            - matchExpressions:
                - key: google.com/edgetpu
                  operator: In
                  values:
                    - "true"
    resources:
      limits:
        google.com/edgetpu: 1
    extraVolumes:
      - name: dri
        hostPath:
          path: /dev/dri
    extraVolumeMounts:
      - name: dri
        mountPath: /dev/dri
    service:
      type: LoadBalancer
      port: 80
      annotations:
        tailscale.com/expose: "true"
        tailscale.com/hostname: "frigate"
  valuesFrom:
    - kind: Secret
      name: frigate-config
      valuesKey: config.yaml
