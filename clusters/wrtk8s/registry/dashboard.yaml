apiVersion: v1
kind: ConfigMap
metadata:
  name: registry-grafana-dashboard
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  docker-registry.json: |
    {
      "annotations": {
        "list": []
      },
      "editable": true,
      "fiscalYearStartMonth": 0,
      "graphTooltip": 1,
      "links": [],
      "panels": [
        {
          "title": "Data Pulled",
          "type": "stat",
          "gridPos": { "h": 4, "w": 6, "x": 0, "y": 0 },
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "fieldConfig": {
            "defaults": {
              "unit": "bytes",
              "thresholds": {
                "steps": [
                  { "color": "blue", "value": null }
                ]
              }
            },
            "overrides": []
          },
          "options": {
            "reduceOptions": {
              "calcs": ["diff"],
              "fields": "",
              "values": false
            },
            "textMode": "auto",
            "colorMode": "value"
          },
          "targets": [
            {
              "expr": "sum(registry_http_response_size_bytes_sum{job=~\"$job\"})",
              "legendFormat": "pulled"
            }
          ]
        },
        {
          "title": "HTTP Requests Rate",
          "type": "timeseries",
          "gridPos": { "h": 8, "w": 12, "x": 0, "y": 4 },
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "fieldConfig": {
            "defaults": {
              "custom": {
                "drawStyle": "line",
                "fillOpacity": 10,
                "stacking": { "mode": "normal" }
              },
              "unit": "reqps"
            },
            "overrides": []
          },
          "targets": [
            {
              "expr": "sum(rate(registry_http_requests_total{job=~\"$job\"}[$__rate_interval])) by (handler, method)",
              "legendFormat": "{{method}} {{handler}}"
            }
          ]
        },
        {
          "title": "HTTP Response Codes Rate",
          "type": "timeseries",
          "gridPos": { "h": 8, "w": 12, "x": 12, "y": 4 },
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "fieldConfig": {
            "defaults": {
              "custom": {
                "drawStyle": "line",
                "fillOpacity": 10,
                "stacking": { "mode": "normal" }
              },
              "unit": "reqps"
            },
            "overrides": []
          },
          "targets": [
            {
              "expr": "sum(rate(registry_http_requests_total{job=~\"$job\"}[$__rate_interval])) by (code)",
              "legendFormat": "{{code}}"
            }
          ]
        },
        {
          "title": "HTTP Request Duration (p50 / p90 / p99)",
          "type": "timeseries",
          "gridPos": { "h": 8, "w": 12, "x": 0, "y": 12 },
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "fieldConfig": {
            "defaults": {
              "custom": { "drawStyle": "line", "fillOpacity": 0 },
              "unit": "s"
            },
            "overrides": []
          },
          "targets": [
            {
              "expr": "histogram_quantile(0.50, sum(rate(registry_http_request_duration_seconds_bucket{job=~\"$job\"}[$__rate_interval])) by (le))",
              "legendFormat": "p50"
            },
            {
              "expr": "histogram_quantile(0.90, sum(rate(registry_http_request_duration_seconds_bucket{job=~\"$job\"}[$__rate_interval])) by (le))",
              "legendFormat": "p90"
            },
            {
              "expr": "histogram_quantile(0.99, sum(rate(registry_http_request_duration_seconds_bucket{job=~\"$job\"}[$__rate_interval])) by (le))",
              "legendFormat": "p99"
            }
          ]
        },
        {
          "title": "In-Flight Requests",
          "type": "timeseries",
          "gridPos": { "h": 8, "w": 12, "x": 12, "y": 12 },
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "fieldConfig": {
            "defaults": {
              "custom": { "drawStyle": "line", "fillOpacity": 10 },
              "unit": "short"
            },
            "overrides": []
          },
          "targets": [
            {
              "expr": "sum(registry_http_in_flight_requests{job=~\"$job\"}) by (job)",
              "legendFormat": "{{job}}"
            }
          ]
        },
        {
          "title": "Storage Action Duration (p50 / p90 / p99)",
          "type": "timeseries",
          "gridPos": { "h": 8, "w": 12, "x": 0, "y": 20 },
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "fieldConfig": {
            "defaults": {
              "custom": { "drawStyle": "line", "fillOpacity": 0 },
              "unit": "s"
            },
            "overrides": []
          },
          "targets": [
            {
              "expr": "histogram_quantile(0.50, sum(rate(registry_storage_action_seconds_bucket{job=~\"$job\"}[$__rate_interval])) by (le, action))",
              "legendFormat": "p50 {{action}}"
            },
            {
              "expr": "histogram_quantile(0.90, sum(rate(registry_storage_action_seconds_bucket{job=~\"$job\"}[$__rate_interval])) by (le, action))",
              "legendFormat": "p90 {{action}}"
            },
            {
              "expr": "histogram_quantile(0.99, sum(rate(registry_storage_action_seconds_bucket{job=~\"$job\"}[$__rate_interval])) by (le, action))",
              "legendFormat": "p99 {{action}}"
            }
          ]
        },
        {
          "title": "Storage Actions Rate",
          "type": "timeseries",
          "gridPos": { "h": 8, "w": 12, "x": 12, "y": 20 },
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "fieldConfig": {
            "defaults": {
              "custom": {
                "drawStyle": "line",
                "fillOpacity": 10,
                "stacking": { "mode": "normal" }
              },
              "unit": "ops"
            },
            "overrides": []
          },
          "targets": [
            {
              "expr": "sum(rate(registry_storage_action_seconds_count{job=~\"$job\"}[$__rate_interval])) by (action)",
              "legendFormat": "{{action}}"
            }
          ]
        },
        {
          "title": "HTTP Requests by Instance",
          "type": "timeseries",
          "gridPos": { "h": 8, "w": 24, "x": 0, "y": 28 },
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "fieldConfig": {
            "defaults": {
              "custom": {
                "drawStyle": "line",
                "fillOpacity": 10,
                "stacking": { "mode": "normal" }
              },
              "unit": "reqps"
            },
            "overrides": []
          },
          "targets": [
            {
              "expr": "sum(rate(registry_http_requests_total{job=~\"$job\"}[$__rate_interval])) by (job)",
              "legendFormat": "{{job}}"
            }
          ]
        },
        {
          "title": "Total Cache Storage",
          "type": "stat",
          "gridPos": { "h": 4, "w": 6, "x": 6, "y": 0 },
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "fieldConfig": {
            "defaults": {
              "unit": "bytes",
              "thresholds": {
                "steps": [
                  { "color": "green", "value": null },
                  { "color": "orange", "value": 200000000000 },
                  { "color": "red", "value": 250000000000 }
                ]
              }
            },
            "overrides": []
          },
          "options": {
            "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false },
            "textMode": "auto",
            "colorMode": "value"
          },
          "targets": [
            {
              "expr": "sum(kubelet_volume_stats_used_bytes{namespace=\"registry\"})",
              "legendFormat": "used"
            }
          ]
        },
        {
          "title": "Cache % Used per PVC",
          "type": "bargauge",
          "gridPos": { "h": 4, "w": 12, "x": 12, "y": 0 },
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "fieldConfig": {
            "defaults": {
              "unit": "percentunit",
              "min": 0,
              "max": 1,
              "thresholds": {
                "steps": [
                  { "color": "green", "value": null },
                  { "color": "orange", "value": 0.8 },
                  { "color": "red", "value": 0.9 }
                ]
              }
            },
            "overrides": []
          },
          "options": {
            "orientation": "horizontal",
            "displayMode": "gradient",
            "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false }
          },
          "targets": [
            {
              "expr": "kubelet_volume_stats_used_bytes{namespace=\"registry\"} / kubelet_volume_stats_capacity_bytes{namespace=\"registry\"}",
              "legendFormat": "{{persistentvolumeclaim}}"
            }
          ]
        },
        {
          "title": "Cache Storage by PVC",
          "type": "timeseries",
          "gridPos": { "h": 8, "w": 24, "x": 0, "y": 36 },
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "fieldConfig": {
            "defaults": {
              "custom": {
                "drawStyle": "line",
                "fillOpacity": 10,
                "stacking": { "mode": "none" }
              },
              "unit": "bytes"
            },
            "overrides": []
          },
          "targets": [
            {
              "expr": "kubelet_volume_stats_used_bytes{namespace=\"registry\"}",
              "legendFormat": "{{persistentvolumeclaim}}"
            }
          ]
        }
      ],
      "schemaVersion": 39,
      "templating": {
        "list": [
          {
            "name": "datasource",
            "type": "datasource",
            "query": "prometheus",
            "current": { "selected": true, "text": "Prometheus", "value": "prometheus" }
          },
          {
            "name": "job",
            "type": "query",
            "datasource": { "type": "prometheus", "uid": "${datasource}" },
            "query": "label_values(registry_http_requests_total, job)",
            "multi": true,
            "includeAll": true,
            "allValue": ".*",
            "current": { "selected": true, "text": "All", "value": "$__all" },
            "refresh": 2
          }
        ]
      },
      "time": { "from": "now-6h", "to": "now" },
      "title": "Docker Registry",
      "uid": "docker-registry"
    }
