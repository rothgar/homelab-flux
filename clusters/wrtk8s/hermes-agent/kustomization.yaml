apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - source.yaml
  - release.yaml
  # The HelmRelease installs the CRD; Flux retries the CR apply until the
  # CRD is registered — no explicit ordering needed.
  - hermesagent.yaml
