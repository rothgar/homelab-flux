{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "flux": {
    "fileMatch": ["clusters/.+\\.yaml$"]
  },
  "kubernetes": {
    "fileMatch": ["clusters/.+\\.yaml$"]
  },
  "packageRules": [
    {
      "matchManagers": ["flux", "helm-values", "kubernetes"],
      "automerge": false
    },
    {
      "matchDepNames": ["postgres", "ghcr.io/cloudnative-pg/postgresql"],
      "matchUpdateTypes": ["major"],
      "enabled": false,
      "description": "Skip Postgres major bumps — they require data migration, not a tag bump."
    },
    {
      "matchPackagePatterns": ["^lscr\\.io/linuxserver/"],
      "versioning": "loose",
      "description": "linuxserver tags are <semver>-<gitsha>-lsN; Renovate's default docker versioning treats the tail as opaque and skips bumps. Loose versioning matches on the leading semver."
    },
    {
      "matchDepNames": [
        "ghcr.io/alexta69/metube",
        "ghcr.io/miniflux/miniflux",
        "ghcr.io/paperless-ngx/paperless-ngx",
        "ghcr.io/pdfcrafttool/pdfcraft",
        "itzg/minecraft-server",
        "itzg/minecraft-bedrock-server",
        "automaticrippingmachine/automatic-ripping-machine",
        "rommapp/romm",
        "docker.n8n.io/n8nio/n8n",
        "ghcr.io/open-webui/open-webui"
      ],
      "matchUpdateTypes": ["minor", "patch", "digest"],
      "automerge": true,
      "platformAutomerge": true,
      "description": "Auto-merge minor/patch bumps for stateless leaf apps. Major bumps still require review. Needs GitHub Settings → General → 'Allow auto-merge' enabled at repo level."
    },
    {
      "matchDatasources": ["helm"],
      "matchDepNames": ["onedev"],
      "matchUpdateTypes": ["minor", "patch"],
      "automerge": true,
      "platformAutomerge": true,
      "description": "Auto-merge onedev helm chart minor/patch bumps."
    },
    {
      "matchDepNames": ["lscr.io/linuxserver/plex"],
      "matchUpdateTypes": ["minor", "patch", "digest"],
      "automerge": true,
      "platformAutomerge": true,
      "automergeSchedule": ["after 4:00am and before 5:00am"],
      "timezone": "America/Los_Angeles",
      "description": "Auto-merge plex, but only in the 04:00–05:00 PT window — Plex tags churn frequently and a restart mid-day is disruptive to active streams."
    },
    {
      "matchDepNames": ["nvcr.io/nvidia/vllm"],
      "allowedVersions": "/^(2[0-5]\\.\\d+|26\\.0[0-4])-py\\d+$/",
      "description": "Pin nvcr.io/nvidia/vllm — 26.06 segfaults in cuStreamCreateWithPriority on GB10 with driver 595. Docker versioning can't parse the -py3 suffix, so allowedVersions is a regex allowing 20.xx-py* through 26.04-py*."
    }
  ]
}
